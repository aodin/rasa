# -*- coding: utf-8 -*
from django.conf.urls.defaults import patterns, include, url


# Examples
urlpatterns = patterns('',
    # url(r'^-api', include('app.api.urls')),
    # url(r'^/app/$', 'app.views.index', name='app_index'),
)
